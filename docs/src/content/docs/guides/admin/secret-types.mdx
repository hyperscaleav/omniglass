---
title: Secret types
description: "The Secret Types catalog: the seed-owned registry of secret shapes (snmp-community, basic-auth, and the rest), read-only reference data gated by secret:read, each type declaring the fields a secret of it expects."
---

import SeededSet from '../../../../components/SeededSet.astro';

**Catalog > Secrets** (`/secret-types`, with `secret:read`) is the registry of **secret
shapes**: the `secret_type` a [secret](/guides/admin/secrets/) takes, each declaring the
**fields** a secret of that type expects (for example `snmp-community` or `basic-auth`). The
gate is the point: `secret` is a **sensitive resource**, deliberately **off** the `*:read`
viewer floor, so a plain read-everything viewer sees neither the secrets directory nor this
page; whoever may read secrets may read the shapes they take, and nobody else. (This registry
used to share a page with location types, and the joint fetch meant a viewer without
`secret:read` lost both; the split gives each registry its own page and its own gate. See
[Location Types](/guides/admin/location-types/).)

The page is the registry's directory: a table of its rows, sorted alphabetically by display
name, each showing the **name**, **display name**, and **origin** (**official**, seed-owned).
A type also carries an `id`, a uuid minted by the database, the internal address the handle
resolves to ([ADR-0062](/architecture/decisions/)); the handle is what you type and read.

- Pick a row to open its **detail blade**: the type's declared **fields**, each with its name,
  scalar type, whether the field is itself **secret** (masked and encrypted on every secret of
  the type), and its **origin**. This is what the [Secrets](/guides/admin/secrets/) create
  drawer renders as inputs when you pick the type.
- **The registry is read-only, for everyone.** There is no create, no edit, and no delete here,
  for the owner exactly as for an operator: the registry is **authoritative seed-owned
  reference data**, upserted on every boot (`ON CONFLICT DO UPDATE`), so a release can correct a
  shape and the correction lands everywhere. This is the opposite ownership story from
  [Location Types](/guides/admin/location-types/), whose shipped rows are operator-owned and
  seeded only if absent. See
  [the seed model](/architecture/core-entities/#the-seed-model-forked-templates-versus-canonical-catalogs).
  The shipped set, rendered from the seed itself:

  <SeededSet kind="secret_types" />
- Each type also carries a **sensitivity default** consumed at secret creation: an integration
  type like `oauth2-client` mints an **admin-sensitive** secret (admin/owner-only), a device
  type an operational one. The [Secrets guide](/guides/admin/secrets/) covers what that means at
  reveal time.

Using a type, picking the shape when minting a secret, is the
[secret write](/guides/admin/secrets/) (`secret:create`), not a registry operation. From the CLI
the registry is `omniglass secret-type list` (see the [CLI reference](/reference/cli/)); there
are no write commands because there are no write routes.
