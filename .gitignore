# Binaries and build output
/bin/
/dist/
# A stray built binary in the repo root (anchored so it does not swallow the
# cmd/omniglass package directory).
/omniglass
*.exe
*.test
*.out

# Build artifacts (generated, not committed)
/internal/docsembed/site/
# The Vite build output, embedded into the binary under -tags web.
/internal/webui/dist/
/web/dist/
/web/node_modules/

# Docs site (Astro Starlight)
/docs/node_modules/
/docs/dist/
/docs/.astro/

# Go
/vendor/
coverage.txt
*.prof

# Local env and secrets
.env
.env.local
*.local

# Editor / OS
.DS_Store
.idea/
.vscode/

# Claude Code worktrees (local, never committed)
.claude/worktrees/

# Playwright MCP scratch (screenshots, logs)
/.playwright-mcp/
/web/.playwright-mcp/


# stale embedded-postgres dev dir (no longer produced)
/.dev/
